{
  "name": "copilot-remote",
  "version": "0.1.0",
  "private": true,
  "description": "PWA for remotely controlling GitHub Copilot CLI sessions",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:web\"",
    "dev:server": "npm run dev --workspace=server",
    "dev:web": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web",
    "start": "npm run start --workspace=server"
  },
  "overrides": {
    "serialize-javascript": "^7.0.4"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "license": "Apache-2.0",
  "allowScripts": {
    "node-pty@1.1.0": true,
    "esbuild@0.27.3": true,
    "esbuild@0.25.12": true
  }
}
